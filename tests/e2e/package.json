{
  "name": "@couchtube/e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end test: loads the real extension in headless Chromium with youtube.com faked, drives the phone remote through the relay.",
  "scripts": {
    "test": "node extension.test.js"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
